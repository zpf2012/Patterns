观察者模式：
	当一个对象被修改时，则会自动通知它的依赖对象。
	
实现原理：
	1. 创建一个被观察的对象Subject
	2. 创建三个对象，这三个对象依赖subject（在构造方法中参数使用Subject），当对象被实例化的时候自动添加到被依赖的list中。
	3. 当调用subject对象的set方法时，同时调用update方法。
注意：使用抽象类中的方法update，保证子类一定含有update方法。传递参数传递的是父类对象而非子类对象。